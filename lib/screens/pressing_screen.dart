import 'package:flutter/material.dart';

class PressingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Pressing screen"),
      ),
    );
  }
}
