name: production_automation_web
description: web app
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.1+3

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  auto_size_text: ^3.0.0
  charts_flutter: ^0.12.0
  cupertino_icons: ^0.1.3
  data_table_2: ^2.2.0
  dotted_border: ^2.0.0+2
  fl_chart: ^0.40.6
  flutter:
    sdk: flutter
  flutter_secure_storage: ^5.0.2
  flutter_svg: ^1.1.0
  fluttertoast: ^8.0.8
  get: ^4.6.1
  google_fonts: ^2.2.0
  graphql_flutter: ^5.0.0
  logger: ^1.1.0
  syncfusion_flutter_charts: ^20.1.61

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
